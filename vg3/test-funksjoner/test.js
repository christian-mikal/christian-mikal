//@ts-check

/**
 * 
 * @param {number} a et tall
 * @param {string} b en tekst
 * @param {Array} c en liste med tall
 * @param {boolean} d ja/nei
 * @returns {string}
 */
function test(a,b,c,d) {
    if (d) {
        return 2;
    } else {
        return [];
    }
    return "test";
}

test(1,"olina",[2,4,5],true)